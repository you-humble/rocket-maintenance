type: object
description: Response returned after successfully creating a spacecraft build order.
required:
  - uuid
  - total_price
properties:
  uuid:
    type: string
    format: uuid
    description: UUID of the newly created spacecraft build order.
  total_price:
    type: number
    format: double
    description: Total price of the spacecraft build order.
